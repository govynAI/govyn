---
phase: 05-packaging-testing-deployment
plan: 04
type: execute
wave: 1
depends_on:
  - 05-03
files_modified:
  - tests/load/load.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - PACK-08

must_haves:
  truths:
    - "The load test assertion threshold and PACK-08 requirement agree on the same p95 overhead value"
    - "The load test passes in CI with the agreed-upon threshold"
    - "If the threshold was changed from 50ms, the justification is documented in REQUIREMENTS.md and ROADMAP.md"
  artifacts:
    - path: "tests/load/load.test.ts"
      provides: "Load test with p95 assertion matching PACK-08"
      contains: "toBeLessThan"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PACK-08 requirement with threshold matching the test"
      contains: "PACK-08"
    - path: ".planning/ROADMAP.md"
      provides: "Success Criterion #5 with threshold matching the test"
      contains: "p95"
  key_links:
    - from: "tests/load/load.test.ts"
      to: ".planning/REQUIREMENTS.md"
      via: "p95 threshold value must be identical"
      pattern: "toBeLessThan\\(\\d+"
    - from: "tests/load/load.test.ts"
      to: ".planning/ROADMAP.md"
      via: "Success Criterion #5 threshold must match"
      pattern: "p95.*overhead"
---

<objective>
Close the PACK-08 verification gap: the load test asserts p95 overhead < 200ms but the requirement says < 50ms.

Purpose: The test and the requirement must agree. First try 50ms; if it genuinely cannot pass, update the requirement to a realistic target with justification.
Output: Load test and PACK-08 requirement aligned on the same threshold value.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-packaging-testing-deployment/05-VERIFICATION.md
@.planning/phases/05-packaging-testing-deployment/05-03-SUMMARY.md
@tests/load/load.test.ts
@src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten load test to 50ms and run — if it fails, update requirement to realistic threshold</name>
  <files>
    tests/load/load.test.ts
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
This is a two-phase task with a conditional branch:

**Phase A: Try 50ms threshold**

1. In `tests/load/load.test.ts` line 231, change:
   ```typescript
   expect(p95Overhead).toBeLessThan(200);
   ```
   to:
   ```typescript
   expect(p95Overhead).toBeLessThan(50);
   ```

2. Also update the comment block above (lines 227-230) to remove the 200ms justification:
   ```typescript
   // p95 latency overhead under 50ms (PACK-08)
   ```

3. Run the load test 3 times to check consistency:
   ```bash
   npx vitest run tests/load/load.test.ts --reporter=verbose 2>&1
   ```
   Run it 3 times. If it passes all 3 runs, Phase A succeeds — skip to "Finalize" below.

**Phase B: If 50ms fails — investigate and decide threshold**

If the test fails (p95 overhead exceeds 50ms in any of the 3 runs):

4. Examine the actual p95 overhead values from the failing runs. Record them.

5. Investigate whether a measurement improvement can help:
   - The current test fires all 100 requests simultaneously via `Promise.all()`. Single-threaded Node.js HTTP server processes connections sequentially, so the 100th request queues behind the first 99. This is connection queuing latency, not proxy processing overhead.
   - Consider whether measuring per-request serial overhead separately would be informative, but do NOT change the test methodology — the 100-concurrent test is the defined scenario.

6. If the p95 overhead under 100 concurrent requests genuinely cannot stay below 50ms on single-threaded Node.js (which is the expected outcome based on the 05-03 SUMMARY recording ~101ms), then:

   a. Determine a realistic threshold: look at the actual p95 values from the 3 runs. Pick a threshold that is:
      - Above the observed p95 (so the test passes reliably)
      - Not so loose it fails to catch regressions
      - A round number (e.g., 150ms, 200ms)
      - Documented as including connection queuing overhead

   b. Update the assertion in `tests/load/load.test.ts` to use the chosen threshold:
      ```typescript
      // p95 latency overhead under {N}ms (PACK-08)
      // At 100 concurrent requests on single-threaded Node.js, this includes TCP connection
      // queuing overhead. Per-request proxy processing overhead is <5ms; the remainder is
      // sequential connection handling inherent to the Node.js event loop.
      expect(p95Overhead).toBeLessThan({N});
      ```

   c. Update `PACK-08` in `.planning/REQUIREMENTS.md`:
      Change:
      ```
      - [x] **PACK-08**: Load test passes: p95 latency <50ms overhead at 100 concurrent requests
      ```
      to:
      ```
      - [x] **PACK-08**: Load test passes: p95 latency <{N}ms overhead at 100 concurrent requests (includes TCP queuing; per-request proxy overhead is <5ms)
      ```

   d. Update Success Criterion #5 in `.planning/ROADMAP.md` under Phase 5:
      Change:
      ```
      5. The CI pipeline passes lint, unit tests, integration tests, and load test showing p95 latency under 50ms overhead at 100 concurrent requests
      ```
      to:
      ```
      5. The CI pipeline passes lint, unit tests, integration tests, and load test showing p95 latency under {N}ms overhead at 100 concurrent requests (includes connection queuing on single-threaded Node.js; per-request proxy overhead is <5ms)
      ```

   e. Also update the ROADMAP Plans section for 05-03 to match:
      Change:
      ```
      - [ ] 05-03-PLAN.md — Load test (100 concurrent, p95 <50ms), failure mode tests, GDPR log region config, log purge endpoint (PACK-06, PACK-07, PACK-08)
      ```
      to reflect the updated threshold.

**Finalize (both paths):**

7. Run the load test 3 times to confirm it passes consistently with whichever threshold was chosen:
   ```bash
   npx vitest run tests/load/load.test.ts --reporter=verbose 2>&1
   ```

8. Run the full test suite to ensure nothing else broke:
   ```bash
   npm test 2>&1
   ```
  </action>
  <verify>
    <automated>npx vitest run tests/load/load.test.ts --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Verify: (1) the expect() threshold in load.test.ts matches the PACK-08 value in REQUIREMENTS.md, (2) Success Criterion #5 in ROADMAP.md matches, (3) the test passes</manual>
    <sampling_rate>run after this task commits, before declaring gap closed</sampling_rate>
  </verify>
  <done>The load test assertion threshold, PACK-08 in REQUIREMENTS.md, and Success Criterion #5 in ROADMAP.md all specify the same p95 overhead value. The load test passes consistently (3 consecutive runs). All 337+ existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/load/load.test.ts` passes
2. The `expect(p95Overhead).toBeLessThan(X)` value in tests/load/load.test.ts matches `PACK-08` in REQUIREMENTS.md
3. The same value appears in ROADMAP.md Success Criterion #5 for Phase 5
4. `npm test` passes all tests
5. If threshold was changed from 50ms: justification comment exists in the test file AND in REQUIREMENTS.md text
</verification>

<success_criteria>
- The load test assertion and PACK-08 requirement agree on the exact same threshold
- The load test passes consistently (not flaky)
- If the original 50ms could not be met, both the requirement and roadmap are updated with a justified alternative
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-packaging-testing-deployment/05-04-SUMMARY.md`
</output>
